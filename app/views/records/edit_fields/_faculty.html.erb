<% if f.object.multiple? key %>
  <%= f.input :faculty, hint: 'Enter as full name (e.g. Faculty of Arts)', required: f.object.required?(key) %>
<% else %>
  <%= f.input :faculty, hint: 'Enter as full name (e.g. Faculty of Arts)' , required: f.object.required?(key) %>
<% end %>
