<% if f.object.multiple? key %>
  <%= f.input :grant_number, hint: 'Unless otherwise specified, enter granting agency AND number (e.g. SSHRC 430-2014-00168 )', required: f.object.required?(key) %>
<% else %>
  <%= f.input :grant_number, hint: 'Unless otherwise specified, enter granting agency AND number (e.g. SSHRC 430-2014-00168 )', required: f.object.required?(key) %>
<% end %>
