<% if f.object.multiple? key %>
  <%= f.input :alternative_title, hint: 'Enter title as listed on front matter. Capitalise the first non-article only.', required: f.object.required?(key) %>
<% else %>
  <%= f.input :alternative_title, hint: 'Enter title as listed on front matter. Capitalise the first non-article only.' , required: f.object.required?(key) %>
<% end %>
