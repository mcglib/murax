<% if f.object.multiple? key %>
  <%= f.input :relation, as: :multi_value, label: 'Relation', required: f.object.required?(key) %>
<% else %>
  <%= f.input :relation, as: :text, label: 'Relation', required: f.object.required?(key) %>
<% end %>
