<% if f.object.multiple? key %>
  <%= f.input :local_affiliated_centre, hint: 'Use Centre name as listed on the publication.', required: f.object.required?(key) %>
<% else %>
  <%= f.input :local_affiliated_centre, hint: 'Use Centre name as listed on the publication.', required: f.object.required?(key) %>
<% end %>
