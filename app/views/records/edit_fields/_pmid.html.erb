<% if f.object.multiple? key %>
  <%= f.input :pmid, as: :multi_value, label: 'PMID', required: f.object.required?(key) %>
<% else %>
  <%= f.input :pmid, as: :text, label: 'PMID', required: f.object.required?(key) %>
<% end %>
