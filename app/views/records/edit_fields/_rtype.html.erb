<% if f.object.multiple? key %>
  <%= f.input :rtype, label: 'Type', required: f.object.required?(key) %>
<% else %>
  <%= f.input :rtype, label: 'Type', required: f.object.required?(key) %>
<% end %>
