<% if f.object.multiple? key %>
  <%= f.input :date, hint: 'Enter as YYYY/MM/DD', required: f.object.required?(key) %>
<% else %>
  <%= f.input :date, hint: 'Enter as YYYY/MM/DD' , required: f.object.required?(key) %>
<% end %>
