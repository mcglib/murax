<%= f.input key,
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/local/subjects",
                      'autocomplete' => key }
            },
            required: f.object.required?(key) %>
