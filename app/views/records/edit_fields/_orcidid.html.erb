<% if f.object.multiple? key %>
  <%= f.input :orcidid, as: :multi_value, label: 'ORCID iD', required: f.object.required?(key) %>
<% else %>
  <%= f.input :orcidid, as: :text, label: 'ORCID iD', required: f.object.required?(key) %>
<% end %>
