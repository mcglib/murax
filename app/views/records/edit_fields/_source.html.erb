<% if f.object.multiple? key %>
  <%= f.input :source, hint: 'Enter link to journal website.', required: f.object.required?(key) %>
<% else %>
  <%= f.input :source, hint: 'Enter link to journal website.' , required: f.object.required?(key) %>
<% end %>
