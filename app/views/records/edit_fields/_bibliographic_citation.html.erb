<% if f.object.multiple? key %>
  <%= f.input :bibliographic_citation, hint: 'Use citation obtained from publisher website.', required: f.object.required?(key) %>
<% else %>
  <%= f.input :bibliographic_citation, hint: 'Use citation obtained from publisher website.' , required: f.object.required?(key) %>
<% end %>
