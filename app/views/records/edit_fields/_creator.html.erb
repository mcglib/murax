<% if f.object.multiple? key %>
  <%= f.input :creator, hint: 'Enter as Last, First (enter multiple authors on separate lines, in order of appearance)', required: f.object.required?(key) %>
<% else %>
  <%= f.input :creator, hint: 'Enter as Last, First (enter multiple authors on separate lines, in order of appearance)' , required: f.object.required?(key) %>
<% end %>
