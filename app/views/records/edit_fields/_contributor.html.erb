<% if f.object.multiple? key %>
  <%= f.input :contributor, hint: 'Enter as Last, First. (enter multiple Supervisors on separate lines).', required: f.object.required?(key) %>
<% else %>
  <%= f.input :contributor, hint: 'Enter as Last, First. (enter multiple Supervisors on separate lines).' , required: f.object.required?(key) %>
<% end %>
