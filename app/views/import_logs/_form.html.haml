= form_for @import_log do |f|
  - if @import_log.errors.any?
    #error_explanation
      %h2= "#{pluralize(@import_log.errors.count, "error")} prohibited this import_log from being saved:"
      %ul
        - @import_log.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
