<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h4>Hi <%= @user.name %></h4>
    <p>
    Below is a report of the last import that occured on <%= @batch.finished %> at <TIME>
    </p>
    <h3>Summary</h3>
    <p> <%= @batch.import_log.where(:imported => true).count %> items imported successfully. </p>
    <p>View the imported list of pids <a href="/import_logs/view/<%= @batch.id %>">here</a>
    <h3> Error list</h3>
    <dl>
    <%= @errors.each do |error| %>
        <dt>Pid</dt><dl><%= error.pid %></dl>
        <dt>Title</dt><dl><%= error.title %></dl>
        <dt>Digitool colection code</dt><dl><%= error.digitool_collection_code %></dl>
        <dt>error message</dt><dl><%= error.error %></dl>

        </li>
    <% end %>
    </ul>
    <h6> View items</h6>
    <a href=""><LINK></a>
  </body>
</html>
